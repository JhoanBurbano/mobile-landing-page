---
import axios from 'axios';
import Card from '../components/Card.astro';
import LayoutNavigator from '../layouts/LayoutNavigator.astro';
import {
  SEQUENCE_BLUE_TW,
  SEQUENCE_RED_TW,
} from '../constants/colors.constants';
import portfolio from '../utils/translate.json';
import type { FormEvent } from 'react';

const handleSubmit = (event: FormEvent) => {
  event.preventDefault();
  console.log('Form submitted!');
};

// const portfolio = await axios(
//   'https://burbanostudio-assets.s3.amazonaws.com/portfolio/i18n/en.json',
//   {
//     headers: {
//       pragma: 'no-cache',
//       'cache-control': 'no-cache',
//     },
//   },
// ).then((res) => res.data);
---

<LayoutNavigator title="jsburbano | Home" index={2}>
  <!-- Contact form -->
  <Card title="" className="bg-gray-50" iconTitle="">
    <section class="py-12">
      <div class="max-w-4xl mx-auto px-6 sm:px-10 lg:px-12">
        <h2 class="text-3xl font-extrabold text-blue-800 text-center">
          Contáctanos
        </h2>
        <p class="text-blue-600 text-center mt-2">
          Si tienes alguna pregunta o necesitas más información, no dudes en
          contactarnos.
        </p>

        <form
          onsubmit="handleSubmit"
          class="mt-8 bg-white p-6 rounded-lg shadow-md space-y-6"
        >
          <!-- Nombre -->
          <div>
            <label for="name" class="block text-blue-700 font-medium mb-1"
              >Nombre</label
            >
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full border border-blue-300 rounded-lg p-3 text-blue-900 focus:outline-none focus:ring focus:ring-blue-200"
              placeholder="Ingresa tu nombre"
            />
          </div>

          <!-- Correo -->
          <div>
            <label for="email" class="block text-blue-700 font-medium mb-1"
              >Correo Electrónico</label
            >
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full border border-blue-300 rounded-lg p-3 text-blue-900 focus:outline-none focus:ring focus:ring-blue-200"
              placeholder="Ingresa tu correo electrónico"
            />
          </div>

          <!-- Mensaje -->
          <div>
            <label for="message" class="block text-blue-700 font-medium mb-1"
              >Mensaje</label
            >
            <textarea
              id="message"
              name="message"
              rows="5"
              required
              class="w-full border border-blue-300 rounded-lg p-3 text-blue-900 focus:outline-none focus:ring focus:ring-blue-200"
              placeholder="Escribe tu mensaje aquí"></textarea>
          </div>

          <!-- Botón Enviar -->
          <div class="flex justify-center">
            <button
              type="submit"
              class="bg-blue-600 text-white px-6 py-3 rounded-lg shadow-md hover:bg-blue-700 focus:outline-none focus:ring focus:ring-blue-200"
            >
              Enviar
            </button>
          </div>
        </form>
      </div>
    </section>
  </Card>
</LayoutNavigator>
